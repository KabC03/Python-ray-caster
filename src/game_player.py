import game_entity;


class Player(game_entity.Entity):
    def __init__(self, moveSpeed, sprintSpeed, turnSpeed, xPos, yPos, angle) -> None:
        super().__init__(moveSpeed, sprintSpeed, turnSpeed, xPos, yPos, angle);



        










