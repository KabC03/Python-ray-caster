import game_entity;

PLAYER_FOV = 90;



class Player(game_entity.Entity):
    def __init__(self, moveSpeed, turnSpeed, xPos, yPos, angle) -> None:
        super().__init__(moveSpeed, turnSpeed, xPos, yPos, angle);



        










